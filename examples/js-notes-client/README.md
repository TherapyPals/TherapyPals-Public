# JS Notes Client (Demo)

This is a demo JavaScript client for Therapals mock endpoints. It uses only fake data and does not connect to production systems.

- Reads a sample SOAP note
- Validates shape against schema
- POSTs to a mock endpoint

## Usage
- Install dependencies: `npm install`
- Run: `node index.js`

See `.env.example` for config placeholders.
